<%= form_with(model: user_fund_project, local: true) do |form| %>
  <% if user_fund_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_fund_project.errors.count, "error") %> prohibited this user_fund_project from being saved:</h2>

      <ul>
      <% user_fund_project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :project_id %>
    <%= form.number_field :project_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
