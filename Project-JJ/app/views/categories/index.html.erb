<!DOCTYPE html>

<head>
<style>

	#allcategories {
    font-family: "Trebuchet MS", Arial, Helvetica, sans-serif;
    border-collapse: collapse;
    width: 100%;
}

#allcategories td, #allcategories th {
    border: 1px solid #ddd;
    padding: 8px;
    text-align: center;
}

#allcategories tr:nth-child(even){background-color: #f2f2f2;}



#allcategories th {
    padding-top: 12px;
    padding-bottom: 12px;
    text-align: center;
    background-color: #004C99;
    color: white;
}


</style>
<h3>Search Categories</h3>
<%= form_tag search_categories_path do |form|%>
 		<input class='search full-width' id='searchbar' name='query' placeholder='Search Categories' type='string' autocomplete='off'>
		<div class="field">
  		</div>
<% end %>

</head>
<body>

<div>
<h1>Categories</h1>

<table id ="allcategories"  width=100%  >

  <thead>
    <tr>
      <th>Name</th>      
      <th colspan="3">Action</th>
      
    </tr>
  </thead>

  <tbody>
    <% @categories.each do |category| %>
      <tr>
        <td><%= category.name %></td>
        
        <td><%= link_to 'Show', category %></td>
	<%- if current_user.try(:admin?) %>
		<td><%= link_to 'Edit', edit_category_path(category) %></td>
		<td><%= link_to 'Destroy', category, method: :delete, data: { confirm: 'Are you sure?' } %></td>
	<% end %>
      </tr>
    <% end %>
  </tbody>
</table>
</div>

<br>
<%- if current_user.try(:admin?) %>
<%= link_to 'New Category', new_category_path %>
<% end %>

</html>



 

</body>
